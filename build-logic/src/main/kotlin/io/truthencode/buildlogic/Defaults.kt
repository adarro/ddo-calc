package io.truthencode.buildlogic

/*
 * Default values for build logic.
 */

/**
 * Default JDK version to use for builds.
 *
 * Keep the fallback aligned with the Quarkus 3.39.x compatibility baseline.
 */
const val FALLBACK_JDK_VERSION = "21"

/**
 * Default Scala version to use for builds
 */
const val FALLBACK_SCALA_VERSION = "3"
