namespace io.truthencode.ddo.model.protocol.parsers;

// Optional: main schema (using it creates a .avsc equivalent file)
schema CriticalThreatRange;

/**  */
record CriticalThreatRange {
        /** Lower bound for critical multiplier */
        int? min;
        /** Upper bound for critical multiplier */
        int? max;
        /** Number to multiply the damage roll by */
        int? multiplier;
    }
